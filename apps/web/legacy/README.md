# Legacy web client

This directory preserves the original standalone static prototype for
historical reference. It is not imported, built, deployed, or served by the
production application in `apps/web/`.
