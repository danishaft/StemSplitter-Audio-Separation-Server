# Legacy web client

This directory preserves the original standalone web client for historical
reference. The production API never serves this application.

Build the React application in `frontend/` to create the supported web client
in `frontend/dist/`.
